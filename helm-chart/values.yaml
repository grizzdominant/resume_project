replicaCount: 1


image: grizzdominant/resume-site:cc8da83e4b16b7fbaeb9e9e2f0c5f31981172c33

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: false  # Set to true if using Ingress
  host: resume.local

autoscaling:
  enabled: true  # Or true, depending on your needs
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
