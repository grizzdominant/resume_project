replicaCount: 1


image: grizzdominant/resume-site:e2b62e8a7399f9d0a4ed30506af87afcd08607fc

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: false  # Set to true if using Ingress
  host: resume.local

autoscaling:
  enabled: true  # Or true, depending on your needs
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
