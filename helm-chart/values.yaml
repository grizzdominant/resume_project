replicaCount: 1


image: grizzdominant/resume-site:46219c237b233d9f3cf6faf3c046312b1bbee135

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: false  # Set to true if using Ingress
  host: resume.local

autoscaling:
  enabled: true  # Or true, depending on your needs
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
