replicaCount: 1


image: grizzdominant/resume-site:53208ec4b59a910a22ebcc467a782cdf9ec42cb6

service:
  type: LoadBalancer
  port: 80

ingress:
  enabled: false  # Set to true if using Ingress
  host: resume.local

autoscaling:
  enabled: true  # Or true, depending on your needs
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
